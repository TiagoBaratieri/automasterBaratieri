spring.application.name=automasterbaratieri

spring.datasource.url=jdbc:h2:mem:auto-db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.hibernate.ddl-auto=create-drop

spring.jpa.show-sql=true

# Diz ao Spring para rodar o import.sql logo após criar as tabelas
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# Importando as configurações de e-mail
spring.config.import=classpath:email.properties